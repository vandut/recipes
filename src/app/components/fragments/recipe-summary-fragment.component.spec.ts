/* tslint:disable:no-unused-variable */

import { RecipeSummaryFragmentComponent } from './recipe-summary-fragment.component';

describe('Component: RecipeSummaryFragment', () => {
  it('should create an instance', () => {
    let component = new RecipeSummaryFragmentComponent();
    expect(component).toBeTruthy();
  });
});
